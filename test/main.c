#include "test.h"

int main() {
    // TEST_RUN(entry);
    // TEST_RUN(hof);
    // TEST_RUN(lmap);
    // TEST_RUN(node);
    // TEST_RUN(vec);

    // TEST_RUN(ast);
    TEST_RUN(lex);
    TEST_RUN(tok);

    puts(COL_GREEN "Ok" COL_DEF);

    return 0;
}
